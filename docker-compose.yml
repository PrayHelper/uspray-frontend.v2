services:
  api:
    container_name: prayhelper-client
    build:
      context: .
      dockerfile: Dockerfile
    image: prayhelper-client
    ports:
      - "4001:3000"
    command: ["serve", "-s", "build"]
